import styles from "./certification.module.css"
const Certification = ({ children }) => {
    return(
        <>
            <div>
                {children}
            </div>
        </>
    )
}

export default Certification